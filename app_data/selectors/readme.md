This directory contains css (selectors) defined for use. There are no string CSS in any tests,   
they're all defined here for test readability.
 
Each file corresponds (has same name) to a directory in `tests/`  
